# Validação manual

Verifique que: uma issue válida consulta o MCP e só produz plano; issue inexistente não gera requisitos inventados; MCP indisponível é declarado; conceitos avançados trazem explicação e alternativas; conflitos com decisões são expostos; implementação parcial não é duplicada; e código só é alterado depois de autorização explícita.
